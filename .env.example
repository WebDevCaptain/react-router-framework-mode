# Project Hub Environment Variables Configuration
# Copy this file to '.env' or '.env.production' and configure as needed.

# Application environment configuration
NODE_ENV=production
PORT=3000

# Simulated database encryption key (Example secret key)
DATABASE_SECRET_KEY=super_secure_random_seed_value_for_production_use_only

# Third-party integrations (example)
ANALYTICS_ID=UA-XXXXXXXXX-Y
